# 程序环境和预处理

程序的翻译环境和执行环境

1.翻译环境：源代码被转换为可执行的机器指令

2.执行环境：用于实际执行代码

![截圖 2026-03-08 16.39.08.png](../images/%E7%A8%8B%E5%BA%8F%E7%8E%AF%E5%A2%83%E5%92%8C%E9%A2%84%E5%A4%84%E7%90%86/%E6%88%AA%E5%9C%96_2026-03-08_16.39.08.png)

源文件单独通过编译器(cl.exe)处理生成目标文件 test.c → test.obj(linux) test.c → test.o(gcc)

可执行程序是.exe结尾的

链接库：库函数依赖…

链接器(link.exe)

编译的三个阶段：预编译/预处理 → 编译 → 汇编   然后进入链接

### 编译

**预处理** 选项 `gcc -E test.c -o test.i`  ： `-o`:output 指定输出到 `test.i` 

`gcc  test.c -E` :输出直接显示在terminal

1. 预处理完成之后停下来，预处理之后产生的结果都存放在`test.i`中
2. 预处理阶段把头文件包含进`test.i`  `#include`
3. `#define`定义符号在代码中进行了替换， 然后删除定义行
    
    删除了注释行
    

总结：文本操作

**预处理详解**

1. **预定义符号（可以直接使用）：**
    
    ```c
    __FILE__ //进行编译的原文件
    __LINE__ //文件当前的行号
    __DATE__ //文件被编译的日期
    __TIME__ //文件被编译的时间
    __STDC__ //如果编译器遵循ANSI C，其值为1，否则未定义
    ```
    
    案例：
    
    ![截圖 2026-03-08 18.20.21.png](../images/%E7%A8%8B%E5%BA%8F%E7%8E%AF%E5%A2%83%E5%92%8C%E9%A2%84%E5%A4%84%E7%90%86/%E6%88%AA%E5%9C%96_2026-03-08_18.20.21.png)
    
    ![截圖 2026-03-08 18.20.46.png](../images/%E7%A8%8B%E5%BA%8F%E7%8E%AF%E5%A2%83%E5%92%8C%E9%A2%84%E5%A4%84%E7%90%86/%E6%88%AA%E5%9C%96_2026-03-08_18.20.46.png)
    
    (可用于记录日志…)
    
    ![截圖 2026-03-08 18.24.29.png](../images/%E7%A8%8B%E5%BA%8F%E7%8E%AF%E5%A2%83%E5%92%8C%E9%A2%84%E5%A4%84%E7%90%86/%E6%88%AA%E5%9C%96_2026-03-08_18.24.29.png)
    

1. **`#define`**
    1. **`#define` 定义标识符**
    
    ```c
    #define Max 1000
    #define STR "Hello bit"
    
    //不要在后面加分号，如果比如说写的是#define Max 1000; 那么Max对应的值就变成了'1000;'
    //导致语法问题
    ```
    
    b. **`#define` 定义宏**
    
    > #define 机制包括了一个规定，允许把参数替换到文本中，这种实现方法通称宏(macro)或定义宏(define macro)
    > 
    
    宏的声明方式：
    
    `#define name(parament-list) stuff` 其中`parament-list` 是一个由逗号隔开的符号表，它们可能出现在stuff中
    
    注意：
    
    参数列表的左括号必须与name紧邻
    
    如果两者之间有任何括号存在，参数列表会被解释为stuff的一部分
    
    `#define SQUARE(X) X*X`
    
    ![截圖 2026-03-08 18.44.45.png](../images/%E7%A8%8B%E5%BA%8F%E7%8E%AF%E5%A2%83%E5%92%8C%E9%A2%84%E5%A4%84%E7%90%86/%E6%88%AA%E5%9C%96_2026-03-08_18.44.45.png)
    
    会输出11，因为本质是替换，5+1*5+1（操作符的优先级导致的问题）
    
    所以可以写作`SQUARE(X) ((X)*(X))`
    
    **另一个反例⬇️**
    
    ![截圖 2026-03-08 18.49.06.png](../images/%E7%A8%8B%E5%BA%8F%E7%8E%AF%E5%A2%83%E5%92%8C%E9%A2%84%E5%A4%84%E7%90%86/%E6%88%AA%E5%9C%96_2026-03-08_18.49.06.png)
    
2. `#define`的替换规则
    1. 在调用宏的时候，首先对参数进行检查，看看是否包含任何`#define`定义的符号，如果是，它们首先被替换
    2. 替换文本随后被插入到程序中原来文本的位置。对于宏，参数名被它们的值所替换
    3. 最后，再次对结果文件进行扫描，看看它是否包含任何由`#define`定义的符号。如果是，就重复上述处理过程
3. #和##

**编译** `gcc test.i -s(直接对预处理后的文件进行编译) / gcc test.c -S（对原文件先预处理，然后编译）` → `test.s`

生成了一个`.s`结尾的文件（汇编代码 Assembly language）

总结：把C语言代码转化为汇编代码

语法分析， 词法分析， 符号汇总（**全局**符号汇总，变量名函数名…），语义分析

**汇编** `gcc -c` 汇编.s文件 生成.o文件 `gcc test.s -c`

**总结：把汇编代码转换成二进制文件（机器指令）**

形成了**符号表 关联函数的地址（地址表）如果是call外部的函数，符号表里的地址是无效的**

### 链接

1. 合并段表
2. 符号表的合并和重定位（跨文件找到函数）

Linux环境 elf格式

整体链接，输出一个可执行程序

最终文件只有一个符号表，对应到正确的地址

备注: extern是外部函数的声明

### 运行环境

1. 程序必须载入内存中。在有操作系统的环境中：一般这个由操作系统完成，在独立的环境中，程序的载入必须由手工安排，也可能是通过可执行代码置入只读内存来完成
2. 程序的执行便开始，接着便调用main函数
3. 开始执行程序代码，这个时候程度将使用一个运行时堆栈(stack)，存储函数的局部变量和返回地址，同时程序也可以使用静态(static)内存，存储于静态内存中的变量在程序的整个执行过程一直保留他们的值
4. 终止程序，正常终止main函数；也有可能是意外终止
